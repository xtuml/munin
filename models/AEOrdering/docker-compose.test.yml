version: "3.9"
services:
  aeordering_test:
    image: "ghcr.io/xtuml/aeordering:test"
    build:
      context: .
      target: install-test
      args:
        EXTRA_MASL_ARGS: "-skiptranslator Sqlite"
    ports:
      - "20000:20000"
      - "30000:30000"
      - "40000:40000"
    volumes:
      - "./test_results:/root/test_results"
      - "./config:/root/config"
    entrypoint: ["/usr/bin/timeout", "30s", "AEOrdering_transient_standalone"]
    command: "-log-level Debug -configPath config/ -configFile aeordering_spec_test.json -postinit schedule/test.sch -util Inspector -inspector-port 0"
