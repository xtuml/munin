public service JobManagement::Worker~>jobCompletionConfirmed ( workerId: in UUID::formatted_uuid,
                                                               jobId: in string ) is
begin

  AEOrdering::jobCompletionConfirmed(workerId, jobId);

end service;
